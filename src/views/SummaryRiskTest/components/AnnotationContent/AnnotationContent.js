import React from 'react';
import * as Styled from './AnnotationContent.styled';

const AnnotationContent = ({ children }) => (
  <Styled.AnnotationContent>{children}</Styled.AnnotationContent>
);

export default AnnotationContent;

import styled from 'styled-components';
import { Color } from '../../../../theme/colors';

export const Link = styled.span`
  display: inline;
  color: ${Color.primary};
  cursor: pointer;
`;

export default Link;
